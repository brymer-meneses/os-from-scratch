[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "kernel"
path = "src/kernel.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
volatile = "0.2.6"

